<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="theme-color" content="#224470">

        <title>{% block title %}Guide{% endblock %}</title>

        {% block stylesheets %}
            <link rel="stylesheet" href="{{ preload(asset('/build/base.css', 'endroid_guide')) }}">
        {% endblock %}
    </head>
    <body>
        {% block body %}
            <div class="container-fluid">
                <div class="row">
                    <main class="col-sm-9  col-md-10 pt-3" role="main">
                        {% block content %}

                        {% endblock %}
                    </main>
                </div>
            </div>
        {% endblock %}
        {% block javascripts %}
            <script src="{{ asset('/build/manifest.js', 'endroid_guide') }}"></script>
            <script src="{{ asset('/build/base.js', 'endroid_guide') }}"></script>
        {% endblock %}
    </body>
</html>